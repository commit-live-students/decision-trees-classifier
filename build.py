from numpy import loadtxt
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeClassifier
from sklearn.model_selection import RandomizedSearchCV

def load_data(path, skiprows):
    pass


def myDecisionClassifier(X_train, X_test, y_train, param_grid, n_iter_search, KFold=3, seed=42, **kwargs):
    pass


def finetune_class(X_train, X_test, y_train, param_grid, n_iter_search):
    pass